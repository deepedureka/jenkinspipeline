pipeline{
	agent any
	stages{
		stage('Build'){
			steps('Build'){
				#sh 'mvn clean package'
				sh 'package'				
			}
			post{
				success{
					echo "Now archiving the package ...."
					archiveArtifacts artifacts: '**/target/*war'
				}			
			}
		}
	}
}
